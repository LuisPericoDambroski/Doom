# =============================================================================
# .env.dev - EXCLUSIVO para desenvolvimento com Docker Compose
# Este ficheiro é commitado. A pipeline de deploy usa variáveis da plataforma.
# O desenvolvedor só precisa: pnpm docker:dev (ou docker compose up)
# =============================================================================

NODE_ENV=development
PORT=3000

# Conexão ao Postgres do contentor 'db' (rede Docker)
DATABASE_URL=postgresql://doom:doom_dev_password@db:5432/doom_dev

# Auth (valores seguros apenas para dev local)
JWT_SECRET=doom_secret_key_dev
VITE_APP_ID=loginsite-cseggtqx

# Utilizador de teste para login rápido (apenas dev)
TEST_USER_EMAIL=teste@teste.com
TEST_USER_PASSWORD=teste123
